#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Commit message checking disabled
# No longer enforcing conventional commit format

echo "✅ Commit message hook disabled - all messages accepted"